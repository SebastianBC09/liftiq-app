// Barrel for shared/components. Per architecture doc section 3.2, this will export:
//   - SkeletonOverlayComponent (canvas + keypoints, OnPush)
//   - FeedbackCardComponent
//   - MetricsGaugeComponent
//   - RepCounterComponent
// None are built yet — added as each lands, e.g.:
//   export * from './skeleton-overlay/skeleton-overlay.component';
export {};
