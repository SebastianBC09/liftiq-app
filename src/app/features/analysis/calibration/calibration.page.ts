import { Component } from '@angular/core';
import { IonContent } from '@ionic/angular/standalone';

@Component({
  selector: 'calibration-page',
  standalone: true,
  imports: [IonContent],
  templateUrl: './calibration.page.html',
  styleUrl: './calibration.page.scss',
})
export class CalibrationPage {}
